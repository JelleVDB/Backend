extends ../layout

block content
    h1= title
    h2 gegevens voor <i>#{user.username}</i>
    p #{user.username} met beroep #{user.profession}
        
    form(action="/users/" + encodeURIComponent(user.username.toLowerCase()), method="POST")
        input(name="_method", type="hidden", value="DELETE")
        input(type="submit", value="Delete")