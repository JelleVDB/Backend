h1 Users

p
  a(href="/users/new") Maak een nieuwe user aan:

ul
  - for(var username in userlist) {
    li
      span= userlist[username].username
      a(href="/users/" + encodeURIComponent(userlist[username].username), style="margin:0 10px") Details
      a(class="button",href="/users/edit/"+encodeURIComponent(userlist[username].username)) Edit
  - };

